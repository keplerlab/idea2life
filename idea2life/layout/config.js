var idea2lifeConfig = require('../config');
var CONFIG = {

    logging: idea2lifeConfig.logging,

    // Relative path to directory where XMLS are stored
    dirPath : '/data/'
};

module.exports.LAYOUT_CONFIG  = CONFIG;